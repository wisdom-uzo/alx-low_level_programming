#include "main.h"

/**
 * mul - Returns the value of the multiplication of two numbers.
 * @a: The first value.
 * @b: The second value.
 *
 * Return: 1 if @c is a digit, 0 otherwise.
 */
int mul(int a, int b)
{

	int result;

	result = a * b;

	return (result);
}
